{% extends 'admin/admin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    {% block container %}
<div class="m-4">

    <table class="table ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th> Nom de la catégorie</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-secondary" href="{{ path('categorie_index') }}"> Revenir à la liste des catégories </a>

    <a role="button" class="btn btn-secondary" href="{{ path('categorie_edit', {'id': categorie.id}) }}"> Modifier </a>

    {{ include('admin/categorie/_delete_form.html.twig') }}
</div>
{% endblock %}
{% endblock %}
